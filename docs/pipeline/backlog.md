# Pipeline Backlog

> **Findings & intake register for the documentation-driven-development pipeline.** Two writers:
> **`00-pipeline-manager`** (harvests every invoked skill's findings/recommendations/Outstanding
> Issues after each run, triages every open entry at the start of the next run, and flips
> statuses) and **`00-intake`** (appends `NEW` entries for user-submitted features, bugs, and
> observations). Stage skills never write here — their findings reach this file via the manager's
> harvest step, which is what guarantees a finding stated in a chat summary survives the session
> that stated it. Rows are never deleted: resolved entries flip to `DONE`, rejected ones to
> `REJECTED` with the reason kept.

[↑ Docs index](../INDEX.md) · [Pipeline journal](pipeline-journal.md) ·
[Pipeline README](../../.claude/skills/README.md)

## Lifecycle

```
NEW ──triage──▶ SCHEDULED (rides a named step)  ──that step runs──▶ DONE
        ├─────▶ DEFERRED  (named revisit trigger) ──trigger fires──▶ NEW (re-triage)
        ├─────▶ NEEDS-USER (named decision)       ──user decides───▶ SCHEDULED / REJECTED
        └─────▶ REJECTED  (reason recorded)
IN PIPELINE = the entry's work is the current/next step's explicit target
```

Every open entry carries a live disposition — `SCHEDULED` names the step it rides with,
`DEFERRED` names its revisit trigger, `NEEDS-USER` names the exact decision required. A
Critical/High entry may not be `DEFERRED` without the user's explicit agreement.

## Entries

| ID | Filed | Type | Source | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-03 | finding | [VR-1150](../implementation/verification/VR-1150-vignette-selection.md) (run #3, Low) | IP-1150's package text claims "no test covers zero-overrides," contradicted by the shipped tests — package prose correction only, no functional gap | Low | 07 | Fold into IP-1150's next package-maintenance edit; no dedicated run | DEFERRED |
| BL-0002 | 2026-07-03 | design-question | IP-2010 package (run #1) | Unresolved "aware vs. unaware" divergence-signal design question flagged in the package; must be resolved before/during IP-2010 implementation | High | 07 | **Resolved (run #4):** user chose to instrument an explicit decision-time signal rather than a post-hoc heuristic. `07-implementation-planning` amended `IP-2010` to v1.1 (`custody_confidence_at_decision` captured in `engine/orders.py`'s `_exec_payload()` via a new `engine/custody.py` helper, read back — never recomputed — by `score_belief_truth_divergence`); Architecture Components, Files to Modify, Implementation Tasks, Tests to Add, Definition of Done, Verification Checklist, Risks, and Rollback Considerations all updated; `00-master-build-plan.md`'s stale risk note corrected. Committed `d07ebe8`, pushed, draft PR [#45](https://github.com/Zabin/ZabSpaceExercise/pull/45). No open design question remains on `IP-2010`; still `READY`+authorized, unchanged. | DONE |
| BL-0003 | 2026-07-03 | finding | IP-1140 package (run #1) | FR-6610 trigger/menu divergence documented in IP-1140; needs adjudication | Medium | 09 | **Resolved (run #9):** [`VR-1140`](../implementation/verification/VR-1140-hot-seat-handoff.md) adjudicated the divergence — the shipped manual-button/auto-cycle mechanism does **not** satisfy FR-6610's full intent. `IP-1140` flipped `COMPLETE → VERIFIED` (it accurately documented the gap, no overclaim). The adjudication's outcome is re-filed as a new, higher-severity finding: see `BL-0015`. | DONE |
| BL-0004 | 2026-07-03 | finding | Journal position (runs #0–#3, standing) | The 11 as-built packages (IP-1010…IP-1110) carry `VERIFIED` with no `VR-xxxx` evidence on disk — statuses predate the VR convention | Medium | 09 | **Resolved (run #29):** the project owner's chosen retro-verification sweep is complete — `IP-1010` (run #18) through `IP-1110` (run #29), all 11 as-built packages now carry a formal `VR-xxxx` report. Aggregate result: 8 of 11 clean (routine citation-drift findings only), 2 with one Medium finding each (`IP-1020`'s lifecycle-naming mismatch `BL-0036`; `IP-1100`'s Role-Assignment overclaim `BL-0045`), `IP-1110` with zero findings. No Critical/High anywhere. **All 18 packages on the Master Build Plan now have a formal VR.** | DONE |
| BL-0005 | 2026-07-03 | gate | Authorization round (run #2) | IP-3010 (Research Analytics) was explicitly **not** authorized (MSTR-006 §3); its other blocker (IP-2010 → `COMPLETE`) cleared 2026-07-03 (run #5) — authorization is now the sole remaining blocker | — | 08 | **Resolved (run #9):** batched into run #9's gate check via `AskUserQuestion`; the project owner authorized `IP-3010`. Recorded in the package's own header/Definition-of-Done, Master Build Plan, `packages/INDEX.md`, `ROADMAP.md`. `IP-3010` flips `BLOCKED → READY`, eligible for a future `08-code-implementation` run. | DONE |
| BL-0006 | 2026-07-03 | finding | `08-code-implementation` run (run #5, IP-2010 Outstanding Issue 1) | Cyber orders build their payload in `OrderSystem._plan_cyber`, a separate path from `_exec_payload()` — cyber-effect decisions never carry `custody_confidence_at_decision` and are excluded from `score_belief_truth_divergence`/`score_custody_quality` coverage. In-scope per IP-2010's literal "Files to Modify" (only `_exec_payload` was named), not a defect. | Low | 07 | Defer until either (a) a future package specifically needs cyber-targeted belief-truth-divergence scoring, or (b) `10-integration-review` flags the coverage gap as material | DEFERRED |
| BL-0007 | 2026-07-03 | finding | `08-code-implementation` run (run #5, IP-2010 Outstanding Issue 3) | `spacesim/ui_web/static/index.html` was modified (new assessment panel markup) though not explicitly named in IP-2010's "Files to Modify" — implied by "a new report panel" (mirrors the AAR panel's own markup+JS split), not scope creep, but should be confirmed rather than assumed | Low | 09 | **Resolved (run #11):** [`VR-2010`](../implementation/verification/VR-2010-competency-assessment.md) adjudicated the inclusion as appropriate, mirroring the AAR panel precedent — no scope creep. | DONE |
| BL-0008 | 2026-07-03 | finding | `08-code-implementation` run (run #6, IP-1120 Outstanding Issue 1) | Systemic pattern: `IP-1120`'s own package doc still read `Status: BLOCKED` (and a stale `Dependencies` note) even though `IP-1150` had reached `VERIFIED` in an earlier run (#3) — the Master Build Plan/`packages/INDEX.md` were correctly updated at the time, but the package's own header text wasn't. Fixed in run #6 for `IP-1120` specifically; the systemic risk (other package docs could carry the same class of drift) is the open item. | Low | 10 | **Resolved (run #17):** `10-integration-review` spot-checked all 18 package docs' own Status headers — the predicted drift class **materialized a second time**, on `IP-1150` (filed as `BL-0030`). This entry's own job (the systemic spot-check) is complete; `BL-0030` carries the concrete instance forward. | DONE |
| BL-0009 | 2026-07-03 | finding | `08-code-implementation` run (run #6, IP-1120) | `docs/requirements/03-requirements-traceability-matrix.md`'s `NFR-3100` row was malformed (missing its `Impl. Package` column entirely — 8 pipe-delimited fields where 9 are expected). Discovered and fixed in the same pass while filling in `IP-1120`'s own citation for that row; no further action needed. | Low | — | Fixed in run #6, same row `IP-1120`'s own Test/Impl. Package citation touched — no dedicated follow-up | DONE |
| BL-0010 | 2026-07-03 | finding | `IP-1120` package's own Risks section (authored run #1, late-harvested run #6) | RTM row for `FR-4510` lists its Title column as "Observer view" — a copy/paste defect; the correct title ("Classification banner set and displayed") is what `01-functional-requirements.md`'s own text states. Flagged in `IP-1120`'s Risks at authoring time (run #1) but never harvested into the backlog until now. Explicitly routed to `04-requirements-engineering`'s territory by the package itself — not fixed by `07`/`08`. | Low | 04 | Defer until the next `04-requirements-engineering` pass touches the RTM (no dedicated run justified for a single title-column typo) | DEFERRED |
| BL-0011 | 2026-07-03 | finding | `08-code-implementation` run (run #7, IP-1130 Outstanding Issue 4 / Risks) | The Observer mutating-route guard's completeness isn't self-maintaining — a route added to `ui_web/server.py` after IP-1130 shipped won't automatically appear in `test_observer.py`'s route table or gain the `_reject_observer` guard, silently reopening FR-6510's gap. Not a current defect (all 22 live routes are covered today). | Low | 10 | **Resolved (run #17):** `10-integration-review` independently re-derived the live 25-route table and diffed it against `test_observer.py`'s 21-entry `_MUTATING_ROUTES` table — the 4-route gap is fully accounted for (2 correctly out-of-scope session-creation routes, 2 already-tracked `BL-0023`/`BL-0024` test-coverage gaps, both confirmed functionally rejected by direct code read). No new drift. | DONE |
| BL-0012 | 2026-07-03 | finding | `IP-1130` package's own Risks section (authored run #1, late-harvested run #7) | RTM row for `FR-6510` listed its Title column as "Multi-monitor pop-out windows" — a copy/paste defect; the correct title ("Observer read-only view with no command ability") is what `01-functional-requirements.md:1170` states. Flagged in `IP-1130`'s Risks at authoring time (run #1) but never harvested until now. The row's stale `ui_web/static/app.js` Impl. Package citation (itself a symptom of the same defect, since Observer didn't exist in code before this run) was corrected to `IP-1130` as part of filling in this package's own Test/Impl. Package cells — the Title cell itself is left for `04-requirements-engineering`, per the same routing IP-1130 itself specifies. | Low | 04 | Defer until the next `04-requirements-engineering` pass touches the RTM | DEFERRED |
| BL-0013 | 2026-07-03 | finding | `08-code-implementation` run (run #8, IP-1151) | `spacesim/ui_web/static/app.js`'s `start()` previously ignored the `/start` endpoint's `Ack.ok` field entirely — a refused start (`{ok: false, reason: ...}`) was silently swallowed and the client proceeded as if it had succeeded. Discovered and fixed in run #8 because `IP-1151`'s new server-side staffing gate is meaningless if the client never surfaces a refusal; the fix is a load-bearing part of this package's own Definition of Done, not a rider. | Low | — | Fixed in run #8, same file `IP-1151`'s own UI wiring touched — no dedicated follow-up | DONE |
| BL-0014 | 2026-07-03 | finding | `08-code-implementation` run (run #8, IP-1151 Implementation Tasks item 6 / Risks) | `IP-1151`'s own text claimed the Role Assignment records it produces are "already `VERIFIED`" as consumable by a command-filtering mechanism in `FS-105`/`IP-1050`/`IP-1051` — checked against the live code during implementation (grep across `FS-105-spacecraft-operations.md`, `IP-1050`/`IP-1051`, `buscommands.py`, `session/manager.py` for "role", case-insensitive: zero matches) and found **false**. No role-based (bus/payload/both) command-authorization concept exists anywhere in the codebase; every existing command check is `cell`-based (blue/red/white ownership). `FS-115`'s own Scope explicitly excludes runtime enforcement from this Feature (so `IP-1151` itself is not out of scope), but the Role Assignment record now has no consumer at all, and no FS currently specifies one. | Medium | 06 | Revisit the next time `06-feature-specification` touches `FS-105`, or if a future package proposes runtime role enforcement — decide then whether to specify FS-105-side consumption or accept the record as advisory-only indefinitely. Not blocking any current work; `IP-1151` stands on its own scope. | DEFERRED |
| BL-0015 | 2026-07-03 | finding | [VR-1140](../implementation/verification/VR-1140-hot-seat-handoff.md) (run #9, adjudicating `BL-0003`) | **The shipped `IP-1140` mechanism does not satisfy FR-6610's full intent.** The hard postcondition (no leaked content) holds, but the trigger is a manual button click, not automatic seat-relinquish detection — in the one Feature in the catalog where fog-of-war is enforced client-side with no server-side backstop, an operator who forgets to click ⏸ Handover before stepping away leaves their cell's content exposed indefinitely, with no system-side prompt. `IP-1140` itself is `VERIFIED` (it documented this gap accurately, no overclaim); this finding is against the *product*. | **High** | 07 | **Resolved (run #10):** batched into run #10's gate check (chat-based, `AskUserQuestion` was unavailable due to a transient tool error). The project owner explicitly accepted the risk rather than authorizing a gap-closing package: "I accept the risk of a cell not blanking the screen during handover as long as hot seat is an option." Recorded in `IP-1140`'s own header/Risks and Master Build Plan Risk item 6. **Spot-checked (run #17):** `10-integration-review` reconfirmed the accepted-risk framing is unchanged in both the package header and Master Build Plan Risk item 6 — no new information reopens it. | DONE |
| BL-0016 | 2026-07-03 | finding | [VR-1140](../implementation/verification/VR-1140-hot-seat-handoff.md) (run #9, Findings #2) | `IP-1140`'s cited file/line ranges (`app.js:2717-2733`/`:2752`, `index.html:596-601`) had drifted from the current tree (now `app.js:2805-2821`/`:2840`, `index.html:640-645`) — content at the new locations matches exactly; pure citation drift from `IP-1130`/`IP-1151` landing code above this block in the same session, not a functional defect. Corrected in `IP-1140`'s own Verification Checklist this pass; the citations in `Files to Create`/reference-files section itself were left as historical record per this pipeline's read-only-package convention. | Low | 07 | Fold into `IP-1140`'s next package-maintenance edit, if any; no dedicated run | DEFERRED |
| BL-0017 | 2026-07-04 | finding | `08-code-implementation` run (run #10, IP-3010 Outstanding Issue 1 / Risks) | `IP-3010`'s own `Files to Create` field cited `tools/build_coastlines.py` as the precedent for `spacesim/tools/research_batch.py`'s location — imprecise: `tools/build_coastlines.py` actually lives in the non-package repo-root `tools/` directory, not inside `spacesim/`. `spacesim/tools/` is a genuinely new subpackage (this package's first use), needed so `run_batch()` is importable by `spacesim/tests/`. Does not change the correctness of the path the package instructed (followed exactly), only the accuracy of the analogy used to justify it. | Low | 07 | Fold into `IP-3010`'s next package-maintenance edit, if any; no dedicated run | DEFERRED |
| BL-0018 | 2026-07-04 | finding | `08-code-implementation` run (run #10, IP-3010 Outstanding Issue 2 / Risks) | `IP-3010`'s `RunRecord` schema was implemented against `IP-2010`'s actual output shape while `IP-2010` is `COMPLETE`, not yet `VERIFIED`. If `IP-2010`'s own `09-package-verification` pass surfaces a material finding against its scoring-function signatures or tier-value sets, `IP-3010`'s schema (and its tests' golden-default assertions) would need revisiting. | Low | 09 | **Resolved (run #11):** [`VR-2010`](../implementation/verification/VR-2010-competency-assessment.md) confirmed `assessment_report`'s signature/output shape is unchanged from what `IP-3010` was built against — no impact on `IP-3010`'s shipped schema. | DONE |
| BL-0019 | 2026-07-04 | finding | [VR-2010](../implementation/verification/VR-2010-competency-assessment.md) (run #11, Findings #1) | FS-201's own Acceptance Criteria state "a longitudinal per-trainee report aggregates dimension-by-dimension results across exercises" — entirely unimplemented by `IP-2010`. Not a surprise: `IP-2010`'s own Implementation Tasks item 5 already, knowingly deferred this (needs a trainee-identity/cross-session model that doesn't exist). The finding is that FS-201 itself is marked `✅ Done` while carrying an unimplemented Acceptance Criterion. | Medium | 06 | Reconcile FS-201's Acceptance Criteria against what `IP-2010` actually delivered — either narrow the criterion to a future Feature, or track it explicitly until a future `IP-2011`-equivalent package closes it. Not blocking any current work. | DEFERRED |
| BL-0020 | 2026-07-04 | finding | [VR-2010](../implementation/verification/VR-2010-competency-assessment.md) (run #11, Findings #2) | FS-201's own Acceptance Criteria state the report must be "accessible in all three assessment modes: automated in-engine, facilitator rubric (FS-106), and self-assessment/debrief (FS-107)." The shipped panel is White-Cell-only (`index.html`'s `white-only` CSS class) — Blue/Red operators have no in-UI path to their own rubric. Unlike `BL-0019`, this exclusion was never flagged anywhere in `IP-2010`'s own text — appears missed at authoring time, not deliberately scoped out. | Medium | 06 | Confirm with `06-feature-specification` whether self-assessment-mode access was actually intended for this iteration (then route a small gap-closing package via `07-implementation-planning` if so) or should be descoped alongside `BL-0019`. Not blocking any current work. | DEFERRED |
| BL-0021 | 2026-07-04 | finding | [VR-2010](../implementation/verification/VR-2010-competency-assessment.md) (run #11, Findings #3) | `IP-2010`'s own `Files to Modify` proposed `confidence_at_decision(world, cell, target, now)`; the shipped signature is `confidence_at_decision(tracks: list[Track], cell, target, now)` — takes the track list directly to avoid an import cycle (`custody.py` is imported by `world.py`). Documented inline in the function's own docstring; functionally equivalent. | Low | — | Informational only — no action needed, already self-documented in the code. | DONE |
| BL-0022 | 2026-07-04 | finding | [VR-1120](../implementation/verification/VR-1120-classification-banner.md) (run #13, Findings #1) | `IP-1120`'s own Definition of Done text refers to "`aar.export_json`" as a distinct function; no such function exists in `aar.py` — the JSON export is `ui_web/server.py`'s `/api/sessions/{sid}/aar/export.json` route, which dumps the same `AARReport` pydantic model `export_csv` reads. Functionally correct (the field round-trips either way); purely a naming imprecision in the package's own prose. | Low | — | Informational only — no action needed. | DONE |
| BL-0023 | 2026-07-04 | finding | [VR-1130](../implementation/verification/VR-1130-observer-read-only-access.md) (run #14, Findings) | Investigating `BL-0011`'s predicted maintenance-drift risk found the two mutating routes added since `IP-1130` shipped (`/roles/assign` from `IP-1151`, and `IP-1130`'s own `/observer/view` POST) are both correctly protected — but via a stricter White-Cell-only allowlist check in `session/inprocess.py`, not `_reject_observer`'s denylist, and neither has an explicit `cell="observer"` rejection test. Functional risk nil (confirmed correct by reading); pure test-coverage gap. | Low | 08 | Fold into the next `08-code-implementation` touch of either route's own module, or a small dedicated test-coverage package; no dedicated run justified today | DEFERRED |
| BL-0024 | 2026-07-04 | finding | [VR-1151](../implementation/verification/VR-1151-seat-role-assignment.md) (run #15, Findings) | `assign_role`'s White-Cell-only gate (`session/inprocess.py`, `if cell != "white": return Ack(ok=False, ...)`) is tested via `test_only_white_cell_can_assign_roles`, which exercises `cell="blue"` — not `cell="observer"` specifically. Same family as `BL-0023`: functional risk effectively nil (the check is a simple equality test against the literal string `"white"`, trivially covering `"observer"` too), but explicit coverage is missing. | Low | 08 | Fold into the same future maintenance pass `BL-0023` already names for `test_session_setup.py`/`test_observer.py`; no dedicated run justified today | DEFERRED |
| BL-0025 | 2026-07-04 | research-gap | Training-corpus elevation (owner, 2026-07-04) | R600 tier (Training Pedagogy & Instructional Design) scaffolded — index + 8 planned topics — but 0 authored. Downstream manual/vignette skills currently cite R600 as "pending" (NFR-3600, RTM). Author R601 first (everything depends on it). | Medium | 02 | **Resolved (run #16):** `02-research-training-pedagogy` authored the full tier (R601→R608, dependency order) at the user's explicit request. All 8 topics carry §2 Scope, inline citations, `### Sources` subsections, and frontmatter per `10-sources-and-methodology.md`. RTM/NFR-3600 Research cells filled in the same run. Re-filed as `BL-0028` (formal WebFetch verification pass, this session's WebFetch was policy-blocked). | DONE |
| BL-0026 | 2026-07-04 | design-question | Training-corpus elevation (owner, 2026-07-04) | FR-11110 mandates role-scoped *coverage*, not the current per-cell monolith layout (`training/12–14`). Whether to restructure the corpus (e.g. task-oriented modules cutting across cells) is an architecture/requirements-led decision, deferred until R600 (esp. R606 minimalist docs) grounds it. | Low | 03 | **Resolved (run #19):** `08-training-manual-authoring` delivered the evaluation as `training/15-manual-traceability.md` §15.6, judged against R606's task-completion-speed criterion. **Recommendation: keep the per-cell layout** (it already satisfies FR-11110 and R606 for the large majority of sections) — a full restructuring is not warranted. Applied in the same pass: lightweight "see also" cross-links added between the three genuinely mirror-image Blue/Red sections (`BLU-9`↔`RED-6` cyber, `BLU-3`↔`RED-3` previews, `BLU-4`↔`RED-4` SDA) to get the cross-cutting benefit without full restructuring cost. Revisit trigger recorded in §15.6 itself, re-filed as `BL-0035`. | DONE |
| BL-0027 | 2026-07-04 | doc-defect | Training-corpus elevation (owner, 2026-07-04) | Assumption A12 names the residual risk that training-artifact currency is procedural (no automated gate beyond `test_vignette_tutorials.py`). Consider extending the playbook-test pattern to manual `> Sources:` anchors so a moved code path fails a test. | Low | 04 | **Concretized (run #17):** `10-integration-review`'s documentation-coherence pass found A12's risk is not hypothetical — see `BL-0029` (5 of 7 tranche-2 features entirely undocumented in the training corpus). This entry's proposed mechanical fix (Sources-anchor test) remains a separate, still-open idea for a future `04`/`08` pass; `BL-0029` carries the concrete instance forward. | DEFERRED |
| BL-0028 | 2026-07-04 | finding | `02-research-training-pedagogy` run (run #16) | This session's `WebFetch` tool was blocked by the environment's egress policy (403 organizational-policy denial, confirmed via the proxy status endpoint — not a transient error) for the entire R600-authoring pass. All 8 R601-R608 topics' citations rest on multi-source WebSearch corroboration (author/title/venue/year/finding cross-checked across ≥2 independent live results per claim) rather than the corpus's normal fetch-and-confirm adversarial verification pass (`10-sources-and-methodology.md` §5.3). | Medium | 02 | A future session with unrestricted WebFetch should run the standard verification pass against all 8 R600 topics (fetch each cited URL, confirm the claim, spot-check Wayback availability) before the tier is relied on with the same confidence as R100/R200/R300. Not blocking current use — multi-source corroboration is solid grounding, just not the formal gate. | DEFERRED |
| BL-0029 | 2026-07-04 | finding | [integration-review-18-package-tranche](../reviews/integration-review-18-package-tranche.md) (run #17, Finding 1) | Five of the tranche's seven tranche-2/forward-design features (`IP-1120` Classification Banner, `IP-1130` Observer Read-Only Access, `IP-1151` Seat-to-Role Assignment, `IP-2010` Competency Assessment, `IP-3010` Research Analytics) have zero mentions anywhere in the per-cell training manuals (`docs/training/12-14`) and no row in `training/15-manual-traceability.md` §15.1 — confirmed by direct case-insensitive search, not inference. `IP-1140` (Hot-Seat Hand-Off) is the one tranche-2 feature that *is* documented (`WCM-3`), showing this is a gap, not policy. Concretizes `BL-0027`'s previously-abstract A12 risk into a scoped, owned instance. | **Medium** | 08 | **Resolved (run #19):** `08-training-manual-authoring` authored coverage for all five: `WCM-1` (Observer), `WCM-2` (Classification, Seat-to-role staffing), new `WCM-11` (Competency assessment), new `WCM-12` (Offline research batch exports). All five gained `training/15` §15.1 rows; `02-interface.md`'s stale "three cells" framing fixed to four (Observer). `spacesim/tests/test_vignette_tutorials.py` re-run green (16 passed, no regression). | DONE |
| BL-0030 | 2026-07-04 | finding | [integration-review-18-package-tranche](../reviews/integration-review-18-package-tranche.md) (run #17, Finding 2) | `IP-1150`'s own package-doc header (`docs/implementation/packages/IP-1150-vignette-selection.md`) still reads `Status: COMPLETE` and calls `IP-1120`/`IP-1151` `BLOCKED` in its `Referenced By` field, though `VR-1150` (run #3) verified it and both downstream packages have been `VERIFIED` since runs #13/#15. Master Build Plan and `packages/INDEX.md` are both correct — materializes `BL-0008`'s predicted drift class a second time. | Low | 07 | Fold into `IP-1150`'s next package-maintenance touch, mirroring `BL-0008`'s own precedent (fixed for `IP-1120` in run #6); no dedicated run justified today | DEFERRED |
| BL-0031 | 2026-07-04 | finding | [integration-review-18-package-tranche](../reviews/integration-review-18-package-tranche.md) (run #17, Finding 3) | `docs/features/feature-index.md`'s descriptions for `FS-112`/`FS-113`/`FS-114`/`FS-115` still end "...; build status unverified" — stale prose from before this tranche's `07`/`08`/`09` passes resolved that exact uncertainty. The `✅ Done` status column itself is accurate; only the trailing clause is stale. | Low | 06 | Fold into the next `06-feature-specification`/`05-feature-decomposition` touch of the feature index; no dedicated run justified today | DEFERRED |
| BL-0032 | 2026-07-04 | finding | [VR-1010](../implementation/verification/VR-1010-mission-planning.md) (run #18, Findings 1-2) | `IP-1010`'s own line-number citations have drifted from the current tree: `orders.py` by a consistent +5 (later unrelated insertions earlier in the file), `inprocess.py` by inconsistent +34/+58/+58/+3 (from `IP-1130`/`IP-1151` landing code above this block). Content at the new locations confirmed correct in both cases; `validate_order()`'s citation of `_locked_read()` is also imprecise (the live code calls `_locked()`, though both wrap the identical `mgr._lock`, so the substantive claim holds). | Low | 07 | Fold into `IP-1010`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016` (`IP-1140`). | DEFERRED |
| BL-0033 | 2026-07-04 | finding | [VR-1010](../implementation/verification/VR-1010-mission-planning.md) (run #18, Finding 3) | `FR-3110`/`FR-3410`/`FR-1220`'s RTM `Impl. Package` cells remain file-level (`engine/orders.py`, `engine/access.py`), not attributed to a specific `IP-xxxx`, because these files are genuinely shared across multiple as-built packages (`IP-1010`+`IP-1020` for orders.py; `IP-1010`+`IP-1040`+`IP-1050`+`IP-1051` for access.py) and only `IP-1010` has been retro-verified so far in the `BL-0004` sweep. | Low | 04 | **Partially resolved (run #20):** `orders.py`'s `FR-3110`/`FR-3410` `Test` cells now cite both `IP-1010` and `IP-1020` (both retro-verified) — the `orders.py` half of this finding is closed. `access.py`'s `FR-1220` remains open, shared with `IP-1040`/`IP-1050`/`IP-1051`, still awaiting their own retro-verification. | DEFERRED |
| BL-0034 | 2026-07-04 | finding | [VR-1010](../implementation/verification/VR-1010-mission-planning.md) (run #18, Finding 4) | `NFR-1500`'s RTM row was malformed — missing its `Impl. Package` column entirely (8 of 9 expected fields), the same defect class as `BL-0009`'s `NFR-3100` fix (run #6). | Low | — | Fixed same run (`VR-1010`'s own pass) — added `engine/simulation.py`, mirroring sibling row `FR-1120`'s citation for the same invariant. No follow-up needed. | DONE |
| BL-0035 | 2026-07-04 | recommendation | `08-training-manual-authoring` run (run #19, `training/15-manual-traceability.md` §15.6) | The per-cell-vs-task-oriented layout evaluation (`BL-0026`) recommended keeping the per-cell layout, but named an explicit revisit trigger: if the corpus grows enough that a single manual exceeds `NFR-3500`'s module-count guidance, or a future role (beyond White/Blue/Red/Observer) needs a mechanic spanning more than two cells, the "small number of genuine mirrors" premise the recommendation rests on would no longer hold. | Low | 08 | Revisit only if either named trigger fires — no current work depends on this; not ripe today | DEFERRED |
| BL-0036 | 2026-07-04 | finding | [VR-1020](../implementation/verification/VR-1020-command-scheduling.md) (run #20, Finding 1) | `IP-1020`'s own Objective/Definition-of-Done claim a "four-state lifecycle" (`queued→executing→executed-unconfirmed→confirmed`) — none of `executing`/`executed-unconfirmed`/`confirmed` exist anywhere in the code as order-lifecycle values (confirmed by grepping the whole `spacesim/` tree). The actual mechanism: `Order.status` stores only `queued`/`rejected`/`cancelled`; `SessionManager.list_orders()` computes a derived, display-only `"executed"` label once the clock passes the window start, safe only via the sub-stepped-clock invariant. The functional guarantee (states are real and observable, not collapsed to instant) holds; the specific vocabulary does not match the code. | Medium | 07 | Next touch of `IP-1020` should restate the Objective/DoD in the actual 3-status-plus-derived-display-label terms; check whether `FS-102` itself uses the same invented vocabulary and needs the same correction | DEFERRED |
| BL-0037 | 2026-07-04 | finding | [VR-1020](../implementation/verification/VR-1020-command-scheduling.md) (run #20, Finding 2) | `orders.py` line citations drifted: a consistent +5 for six of seven cited functions (same drift class `VR-1010` found), but `_plan_cyber` drifted +27 (`IP-2010`'s v1.1 cyber-payload code landed between it and `_plan_collection`). Content confirmed correct at the new locations. | Low | 07 | Fold into `IP-1020`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016`/`BL-0032`. | DEFERRED |
| BL-0038 | 2026-07-04 | finding | [VR-1030](../implementation/verification/VR-1030-custody-management.md) (run #21, Finding 1) | `custody.py`'s `observe()` citation drifted `:57→:78` (+21 lines) — `IP-2010` v1.1's `confidence_at_decision()` helper was inserted immediately above it. `orders.py`'s gate citation drifted `:349→:353-355` (minor). Content confirmed correct at both new locations. | Low | 07 | Fold into `IP-1030`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016`/`BL-0032`/`BL-0037`. | DEFERRED |
| BL-0039 | 2026-07-04 | finding | [VR-1040](../implementation/verification/VR-1040-sda-tasking.md) (run #22, Finding 1) | `orders.py`'s three cited functions (`_plan_collection`, `_candidate_sensors`, `_contended`) drifted +5 lines each — the same file-wide drift `VR-1010`/`VR-1020`/`VR-1030` already found. `ssn.py`'s own citations had zero drift. Content confirmed correct throughout. | Low | 07 | Fold into `IP-1040`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016`/`BL-0032`/`BL-0037`/`BL-0038`. | DEFERRED |
| BL-0040 | 2026-07-04 | finding | [VR-1050](../implementation/verification/VR-1050-spacecraft-operations-bus-payload.md) (run #23, Finding 1) | `orders.py`'s `_exec_payload (:562-563)` citation actually points at an interior `if order.action == "command":` branch inside that function (now at `~:585`), not the function's own definition line (`:399`) — an imprecise citation style (pointing at an interior branch) compounding with ordinary drift, not a new problem class. `buscommands.py`'s own citations drift +2 to +26 depending on proximity to later insertions. Content confirmed correct at the real locations. | Low | 07 | Fold into `IP-1050`'s next package-maintenance touch; flag the interior-branch-citation style to `07-implementation-planning` as a pattern to avoid in future packages | DEFERRED |
| BL-0041 | 2026-07-04 | finding | [VR-1051](../implementation/verification/VR-1051-spacecraft-operations-effects-console.md) (run #24, Finding 1) | `IP-1051`'s own "Reference files" section cites `_FREQ_HOP_RESIDUAL`/`_EVASION_RESIDUAL` as living in `spacesim/engine/orders.py:89-100` — they actually live in `spacesim/engine/effects.py:101-102` and always have (zero hits for either identifier anywhere in `orders.py`). A genuine file misattribution, not merely drift — though the substantive DoD claim (defender-side modifiers genuinely change the resolver's probability roll) is confirmed true at the real location. | Low | 07 | Correct the file citation from `orders.py` to `effects.py` at `IP-1051`'s next package-maintenance touch; no dedicated run | DEFERRED |
| BL-0042 | 2026-07-04 | finding | [VR-1060](../implementation/verification/VR-1060-white-cell-dashboard.md) (run #25, Finding 1) | All 13 of `IP-1060`'s cited `manager.py` line numbers have drifted by a uniform +32 or +34 lines — a single-cause, file-wide shift (unlike `orders.py`'s multi-stage drift pattern), consistent with one contiguous block of code (likely `IP-1130`'s Observer-designation state or `IP-1151`'s role-assignment registry) landing early in the file since this package was authored. Content confirmed correct at every new location. | Low | 07 | Fold into `IP-1060`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016`/`BL-0032`/`BL-0037`/`BL-0038`/`BL-0039`/`BL-0040`. | DEFERRED |
| BL-0043 | 2026-07-04 | finding | [VR-1070](../implementation/verification/VR-1070-after-action-review.md) (run #26, Finding 1) | `IP-1070`'s three `inprocess.py` AAR-wrapper citations (`aar_report`/`aar_objectives_at`/`aar_snapshot_at`) drifted `:742-750→:800-808` (+58) — the same file-wide `inprocess.py` growth this sweep keeps finding (`BL-0032`, `BL-0039`). `aar.py`'s own 10 citations are essentially unchanged (0-2 line drift). Content confirmed correct at every real location. | Low | 07 | Fold into `IP-1070`'s next package-maintenance touch; no dedicated run. Same drift class as `BL-0016`/`BL-0032`/`BL-0037`-`BL-0040`/`BL-0042`. | DEFERRED |
| BL-0044 | 2026-07-04 | finding | [VR-1090](../implementation/verification/VR-1090-multiplayer-session-transport.md) (run #27, Finding 1) | `IP-1090`'s four `manager.py` citations (`_catch_up_locked`/`_record_catch_up_lag`/`catch_up`/`clock_state`) drifted by a uniform +32 lines — the identical single-cause shift `VR-1060` (run #25, `BL-0042`) already found in this same file. Content confirmed correct at every new location. | Low | 07 | Fold into `IP-1090`'s next package-maintenance touch; no dedicated run. Same file-wide shift as `BL-0042`. | DEFERRED |
| BL-0045 | 2026-07-04 | finding | [VR-1100](../implementation/verification/VR-1100-save-and-resume.md) (run #28, Finding 1) | `IP-1100`'s own Requirements Covered table claims `save_state()`/`from_state()` persist "Role Assignments" — confirmed **false** by direct code read: `role_assignments` is absent from `save_state()`'s returned dict and never restored by `from_state()`, exactly contradicting what `IP-1151`'s own Rollback Considerations already discloses correctly ("`role_assignments` is in-memory `SessionManager` state, not persisted"). No functional defect — nothing relies on Role Assignments surviving save/resume — but the claim itself is false as written. | **Medium** | 07 | Next touch of `IP-1100` should correct the Requirements Covered table to remove the Role Assignments claim | DEFERRED |
| BL-0046 | 2026-07-04 | finding | [VR-1100](../implementation/verification/VR-1100-save-and-resume.md) (run #28, Finding 2) | `manager.py`'s three citations (`add_tle`/`save_state`/`from_state`) drifted by the same uniform +32/+33 this sweep has repeatedly found in this file (`BL-0042`, `BL-0044`). Content confirmed correct at every new location. | Low | 07 | Fold into `IP-1100`'s next package-maintenance touch; no dedicated run. Same file-wide shift as `BL-0042`/`BL-0044`. | DEFERRED |
| BL-0047 | 2026-07-04 | finding | [VR-1100](../implementation/verification/VR-1100-save-and-resume.md) (run #28, Finding 3) | `IP-1100`'s "Tests to Add" section cites `spacesim/tests/test_session.py` as the save-resume round-trip coverage; that file has zero save/resume tests. The actual test (`test_save_resume_reproduces_state_and_queue`) lives in `spacesim/tests/test_session_features.py`. Coverage is genuine, just misattributed to the wrong file — same class as `BL-0022`. | Low | 07 | Correct the file citation at `IP-1100`'s next package-maintenance touch; no dedicated run | DEFERRED |
| BL-0048 | 2026-07-05 | finding | [training-review-runs16-19-scope](../reviews/training-review-runs16-19-scope.md) (run #30, Finding 1) | `WCM-1`/`BLU-1`/`RED-1` (Sources footers) and `training/15-manual-traceability.md` §15.1 ("Fog-of-war boundary & per-cell view" row) + §15.2 (`WCM-1`/`BLU-1`/`RED-1` rows) — seven locations total — cite `spacesim/session/controller.py` as the fog-filter module. That file has never existed (confirmed via `git log --all --diff-filter=A`); the real `CellController` class lives in `spacesim/session/cells.py`. A genuine misattribution, not line-drift — a reader following the Sources footer to confirm the prose would look for a nonexistent file. Underlying behavioral claims in all four sections are accurate. | Medium | 08 | **Resolved (run #32):** `08-training-manual-authoring` corrected all seven citations to `spacesim/session/cells.py` (`CellController`). Playbook suite re-run green (16/16), no regression. | DONE |
| BL-0049 | 2026-07-05 | finding | [release-assessment-fs-tracked-baseline](../reviews/release-assessment-fs-tracked-baseline.md) (run #31, Finding 2) | **`FEAT-3500` (Role-Scoped Command Catalog & Assignment Scoping) — bucketed Must-priority in Release 1 — has no owning Feature Specification and no implementation anywhere in the codebase.** `FR-3510`/`FR-3520` both require rejecting out-of-role commands (at the order panel and at execution time); the RTM shows both fully `UNASSIGNED` (Test + Impl. Package), and this session's own grep confirms `IP-1151`'s `role_assignments` record is read only by `staffing_report()` — nothing consumes it for command authorization. This substantially escalates `BL-0014` (which found the same absence but filed it Medium/routed to a future `06` touch, not recognized as release-blocking undelivered Must-priority scope until this assessment specifically audited "is every planned Feature delivered"). The release plan's own "traceability gap, not new development" framing for `FEAT-3500` does not hold — the capability itself is missing, not merely uncited. | **High** | 06 | **Progress (runs #33-#36):** Path A executed through planning — `FS-116` authored (run #33), `ADS-3500` resolved its two Open Questions (run #34), `FS-116` v1.1 closed them (run #35), `IP-1160` authored (run #36). **Run #37: the project owner was asked for `IP-1160`'s MSTR-006 §3 authorization and answered "not yet."** Remains open — now blocked purely on that authorization decision, not on any remaining spec/architecture/planning work. | SCHEDULED |
| BL-0050 | 2026-07-05 | finding | [release-assessment-fs-tracked-baseline](../reviews/release-assessment-fs-tracked-baseline.md) (run #31, Finding 1) | `FR-3310` ("single scheduler for commands and collection tasks") is not literally implemented as one shared mechanism — `OrderSystem`/`Scheduler` (commands) and `SSNNetwork` (collection requests) are two separate, independently-deterministic subsystems; no `PlannedActivity` class exists anywhere in `engine/` (confirmed by grep). `FS-102-command-scheduling.md` itself explicitly disclaims citing any FR, so no Feature Specification anywhere claims to implement `FR-3310`. Behavioral risk is low (both mechanisms independently confirmed deterministic/replay-safe by `VR-1020`/`VR-1040`), but no FS owns this FR and the "single mechanism" framing doesn't match the shipped architecture. | Medium | 06 | Assign `FR-3310` to `FS-102` (or a new spec) at the next `06-feature-specification` pass touching command scheduling; either soften the requirement's framing to match two independently-verified mechanisms, or add a dedicated cross-kind rewind test if "single mechanism" is load-bearing. Not blocking GO on its own (Medium). | DEFERRED |
| BL-0051 | 2026-07-05 | feature | intake: project owner, filed while `FEAT-3500`/`FS-116`/`IP-1160` was in flight | **Seat-count declaration + role-assignment matrix UI at vignette setup.** Project owner's own words: "Seat IDs within cells will be important for the white cell to assign bus vs payload per assets in a matrix of check boxes on vignette setup / start. Perhaps there should be some additional feature requirements for white cell setup at vignette load which includes where they are asked for how many seats there are per cell so they can assign roles to those seats." Checked against the shipped mechanism: `FR-4210`'s own text ("the system shall allow White Cell to assign each operator seat to one or more roles... informed by the vignette's declared `roles_needed`") is satisfied by today's one-at-a-time `SessionManager.assign_role(seat, asset_or_constellation, role)` binding (`FS-115`/`IP-1151`, `VERIFIED`) — `FR-4210` does not require, and the shipped UI does not have, (a) an explicit "how many seats does each cell have" declaration step before assignment begins, or (b) a matrix/checkbox-grid presentation for assigning roles across multiple seats × assets at once. This is a genuine UX/workflow capability with no home in the current requirements baseline, not a citation gap. No duplicate entry found in this backlog. | — (user's own framing: "perhaps," not stated as urgent) | 06 *(fully absorbed — see Disposition)* | **Triaged run #38: bundled into `BL-0052`'s track.** **Run #40:** `ADS-5100A` specified the seat-count-declaration step and matrix UI. **Run #41:** `06-feature-specification` fully absorbed this into [`FS-117`](../features/FS-117-vignette-creator.md) — no longer tracked as a separate line item. Remaining work rides `FS-117`/`BL-0055` (the requirements-baseline gate). | DONE |
| BL-0052 | 2026-07-05 | feature | intake: project owner, extensive scoping discussion this session (research pass + 4 AskUserQuestion design-fork decisions) | **Vignette Creator — a large, distinct White-Cell authoring feature, anchored to (but explicitly not blended into) `FEAT-5100` (In-App Iterative Vignette Builder, `EP-5000`, 0% built — no FS, no code).** Project owner's own words: "I want a JSON view, a 2D/3D views of the initial state. Review research for all JSON vignette parameters to ensure there is an optimal entry method for the white cell. There should be a TLE entry box, lat/long entry box for ground infrastructure, when entering tle and lat/long a selector for asset type and cell assignment and name entry. ROE selectors per cell. There should be an asset menu where assets can be edited, reassigned, deleted. Payload and bus parameter defaults can be overwritten (eg effective range of payload, fuel level). Identify any missing research that needs to be completed to ensure all bus, payload, and ground infrastructure parameters are captured, listed in the JSON, determines the variable type/range, and available in the vignette creator providing an effective way of entering/editing the information." Explicit phasing: **v1 (this entry's scope) is authoring-time only — no exercise running.** A later, explicitly deferred phase reuses the same/similar menu surface alongside the existing White-Cell inject mechanism for mid-exercise parameter edits and asset add/remove — linked, not in scope now. **Grounding research performed this session** (Explore-agent read of the live tree): EP-5000 catalogs FEAT-5100/5200 (TLE-add endpoint exists, sgp4-validated, pre-start-only, zero UI wiring, no real Space-Track integration despite release-plan's "RTM-verified built" claim)/5300 (spec'd, unimplemented), none built; `CR-11` (already an open Candidate Requirement) asks exactly where in-progress vignette-authoring state should live, unresolved until now; ground stations are plain `Asset` entries with `location:{lat_deg,lon_deg,alt_m}`, no dedicated schema, but `docs/vignettes/GROUND-INFRASTRUCTURE.md` curates ~45 real sites for reuse; all 8 payload types share one identical generic `PayloadState` model, no type-specific fields; ROE is two global `Parameter`s (`red_kinetic_authorized`, `cyber_authorized`) enforced globally in `engine/orders.py` regardless of issuing cell, not actually per-cell; confirmed research gap — no doc enumerates realistic bus/payload parameter ranges, no TLE-format primer, no ground-siting methodology beyond the coordinate list. **Four design-fork decisions the project owner made directly (AskUserQuestion), binding on whoever picks this up:** (1) draft-authoring state is a **server-side draft session** — an unstarted `SessionManager` instance in memory, reusing existing engine/session machinery (TLE-add endpoint, etc.), serialized to YAML on Save; the 2D/3D initial-state preview should reuse the existing render-from-custody scene/globe/map pipeline in ground-truth mode, not a bespoke visualization — this resolves `CR-11`; (2) payload parameters get **typed per-payload-type sub-schemas** (e.g. an ISR sub-schema's `effective_range_km` distinct from SATCOM's bandwidth field), a real Domain Model change, not the generic dict kept as-is — this is exactly where the missing parameter-range research needs to land; (3) **build real per-cell ROE enforcement now** (not a cosmetic per-cell selector over today's global flags) — changes the Domain Model and `engine/orders.py`'s currently-global check, touching already-`VERIFIED` `FS-101`/`FS-102` territory; (4) **documentation route: a new `ADS-xxx` design-synthesis cluster first (Workflow B, like `ADS-3500`), then a consolidated Feature Specification** that likely supersedes `FEAT-5100`'s FS and clarifies its boundary against `FEAT-5200`/`FEAT-5300`, rather than blending into any of the three. **Decided (2026-07-05, follow-up):** Space-Track.org network integration is explicitly **deferred** — out of this feature's v1 scope entirely, left as `FEAT-5200`'s own separate, not-yet-scheduled concern. **TLE entry's primary/only mode for v1 is manual paste**, matching the backend TLE-add endpoint's existing sgp4-validated behavior — no auto-import UI to build now. **Still-undecided items to land in that ADS-xxx/FS pass:** a raw JSON view/editor; lat/long entry for ground infrastructure (should probably offer picking from the existing curated site list, not just free entry); asset-type/cell-assignment/name-entry selectors alongside TLE/lat-long entry; an asset menu (edit/reassign/delete); bus parameter defaults (e.g. fuel level) overridable, same treatment as payload sub-schemas. No duplicate entry found in this backlog. | High (project owner-initiated, extensively scoped before filing — not a passing idea) | 06 *(fully absorbed — see Disposition)* | **Triaged run #38: `02-research-ow-orbital-mechanics` first, then `03-architecture-design-synthesis`.** **Run #39:** research closed. **Run #40:** `ADS-5100A`/`ADS-5100B` authored. **Run #41:** `06-feature-specification` authored [`FS-117`](../features/FS-117-vignette-creator.md), fully consolidating this entry (and `BL-0051`) into one Feature Specification — no longer tracked as a separate line item. `FS-117` itself surfaced a Critical requirements-baseline gap, filed separately as `BL-0055`. Remaining work rides `FS-117`/`BL-0055`. | DONE |
| BL-0053 | 2026-07-05 | research-gap | [R109](../research/encyclopedia/R109-sensor-operations.md) §3 (this session's R1xx grounding pass) | **`weather` and `mw` (missile-warning) payload types have zero `BEAM_MODES` entries in `engine/isr.py`** — unlike `isr_eo`/`isr_sar`/`sda`, `beam_params()` silently falls back to generic EO stripmap numbers (`swath_km`/`resolution_m`/`power_factor`/`duty_cycle`/`gain_factor`) for either type. `R109` grounds realistic real-world numbers for both (GOES-R ABI resolution/revisit for weather; SBIRS-GEO scan/stare characteristics for missile warning) but the engine itself has no parameterization to receive them. Flagged explicitly as a precondition for `BL-0052`'s typed per-payload-type sub-schemas, not merely a nice-to-have — a typed schema over a field the engine doesn't honor would be worse than no field at all. | Medium | 08 | Folds into `BL-0052`'s eventual Implementation Package once `03-architecture-design-synthesis`/`06-feature-specification` reach it — add `BEAM_MODES["weather"]`/`BEAM_MODES["mw"]` entries before or alongside building the weather/mw typed sub-schemas, not after. Not a standalone fix in isolation (no current UI surface exposes payload type selection that would benefit from it yet). | SCHEDULED |
| BL-0054 | 2026-07-05 | research-gap | [R137](../research/encyclopedia/R137-bus-and-payload-parameter-catalog.md) §5 (Bus and Payload Configuration Parameter Catalog, this session) | **Four candidate gaps surfaced while cataloging every bus/payload parameter, none blocking:** (1) `Asset.hardening` (0..1 passive-defense factor) has no topic characterizing what a realistic value is or its numeric effect beyond "lowers safe-mode susceptibility"; (2) `PayloadState.last_effect_assessment` (space-control payload type) is uncharacterized and effectively unused in current vignettes; (3) `PayloadState.deception_active`'s code comment cites the USSF Space Warfighting Framework passive measure #2 but explicitly defers "the downstream effect-resolver change (custody confidence + attribution ambiguity)... as a separate change" that no R1xx topic has since picked up; (4) `Asset.civilian`'s political-cost consequence (`docs/FUTURE-WORK.md` §10.D.16) is declared but not modeled or characterized anywhere. | Low | 02 | Future `02-research-ow-orbital-mechanics` maintenance work, as-needed when a feature actually touches one of these four fields — none currently block any scheduled work. Revisit trigger: the day any Feature Specification proposes making one of these four fields live/authorable. | DEFERRED |
| BL-0055 | 2026-07-05 | finding | [FS-117](../features/FS-117-vignette-creator.md) (Open Question 1, `06-feature-specification` run) | **Critical requirements-baseline gap: most of `FS-117` (Vignette Creator)'s scope has no owning `FR-xxxx`/`NFR-xxxx` at all.** `FEAT-5100`'s Feature Catalog entry lists only `FR-5110`/`NFR-2000` (iterative in-app composition, content-as-data) as Included Requirements — but the two design syntheses grounding `FS-117` (`ADS-5100A`/`ADS-5100B`) commit to substantially more: typed per-payload-type/bus parameter sub-schemas, real per-cell ROE enforcement, and specific UI mechanics (JSON view, 2D/3D preview, TLE/lat-long entry, asset menu, seat/role matrix) — none of which traces to any requirement in `docs/requirements/01-functional-requirements.md`/`02-non-functional-requirements.md`, and none of which matches the existing `CR-01`–`CR-18` Candidate Requirements either (checked directly — `CR-13` is the closest near-miss, but covers *coalition/N-cell* generalization, not making the *existing* two cells' ROE independent). `06-feature-specification` correctly refused to invent FR/NFR numbers to paper over this, per its own rule. | **Critical** | 04 | **Run #42:** the project owner chose to run `04-requirements-engineering` now, rather than split delivery. **Requirements gap closed:** `FR-5120`-`FR-5180`, `FR-3420`, `NFR-2010` added (see [`reviews/requirements-update-fs117.md`](../reviews/requirements-update-fs117.md)), each traced to `ADS-5100A`/`ADS-5100B` or the R1xx research they cite; no invented numbers. Not yet fully `DONE`: `FS-117`'s own `Requirements Implemented` field still needs a follow-up `06-feature-specification` touch to cite the new IDs before this Open Question is literally closed in that document. | SCHEDULED (rides a brief `06-feature-specification` touch to `FS-117` next) |
| BL-0056 | 2026-07-05 | finding | [requirements-update-fs117](../reviews/requirements-update-fs117.md) (Finding 9, `04-requirements-engineering` run #42) | `docs/architecture/05-functional-requirements.md` (GDS-05, this project's stated architecture-ladder-authoritative FR source) was **not** updated when `FR-5120`-`FR-5180`/`FR-3420` were added to `docs/requirements/01-functional-requirements.md` (the "elaborated in full traceability detail" downstream document) — editing GDS-05 is `03-architecture-design-synthesis`'s scope, not `04-requirements-engineering`'s. Named explicitly rather than left as undetected drift between the two documents. | Medium | 03 | Fold into a future `03-architecture-design-synthesis` maintenance touch to GDS-05 (the same kind of merge-gate absorption GDS-05 already did for `build-spec/02` §5-6) — not urgent enough to justify a dedicated run on its own; revisit the next time GDS-05 is touched for any reason, or before a formal audit of GDS-05 vs. `docs/requirements/01`'s consistency. | DEFERRED |
