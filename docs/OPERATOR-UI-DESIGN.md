# Operator UI design — relocated

This document has been **retired**. Its content was split between the v1 spec and the future-work
TODO so that the live spec stays single-source-of-truth:

- **Implemented v1 console** (principles, screen architecture, fleet rail, subsystem drill-down,
  command compose, recovery strip, button-state contract with the validator's reason strings,
  catalog verbs, data/API binding, accessibility): see **`00-BUILD-SPECIFICATION.md` §16**.
- **Deferred / v2 work** (remaining catalog-verb gaps, constellation aggregation, APP-6
  symbology pack, Δv panel, full Cesium globe, multiplayer transport, SSN, etc.): see
  **`docs/FUTURE-WORK.md`**.

The Space-Surveillance-Network design (`docs/SSN-DESIGN.md`) is referenced from `FUTURE-WORK.md`
§7; it remains the detailed proposal for an SSN that is not yet wired into the engine or UI.
